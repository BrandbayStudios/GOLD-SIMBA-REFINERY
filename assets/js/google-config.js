/* ==========================================================================
   EventHub — Google Maps Platform configuration
   --------------------------------------------------------------------------
   Fill this in with YOUR OWN Google Cloud project's API key.
   See GOOGLE_SETUP.md in the project root for exact steps.

   This key is visible in the page source — that is normal for browser
   Maps/Places keys. Restrict it in the Google Cloud Console (HTTP
   referrers = your domain, API restrictions = Maps JavaScript API +
   Places API) so it can't be used from anywhere else or for anything
   else.
   ========================================================================== */

window.EH_GOOGLE_MAPS_API_KEY = "YOUR_GOOGLE_MAPS_API_KEY";
